import styled from 'styled-components'

export default styled.ul`
  list-style: none;
  padding: 0;
  padding-top: ${p => p.theme.sizes.lg};
  padding-bottom: ${p => p.theme.sizes.lg};
  margin: 0;
`
